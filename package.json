{
  "name": "manabu-Allergies",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/pwa": "^3.3.5",
    "core-js": "^3.9.1",
    "npm": "^7.19.0",
    "nuxt": "^2.15.3"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
